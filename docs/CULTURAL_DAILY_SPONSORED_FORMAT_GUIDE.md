# Cultural Daily — Sponsored (Our Friends) format guide

_This file is generated by `scripts/audit_sponsored_last_year.py`. Regenerate after refreshing audits._

## Purpose

Train operators and automation on **what we actually publish**: sponsored posts by **Our Friends** on Cultural Daily. 
This is **not** a license to invent layout—**`CLAUDE.md`**, **`CRITICAL_RULES.md`**, and **`QA.md`** still win when they conflict. 
Use this guide to spot **drift** (a new Google Doc or draft that looks nothing like recent live posts).

## Data sources (pipeline mental model)

| Stage | What we measure | Where |
|--------|-----------------|--------|
| **Client input** | Google Doc HTML export | URLs in `data/training_docs.txt`; batch parse `python3 doc_parser.py --batch …` |
| **This audit** | Final `content.rendered` on **published** posts | REST `after=` window, author filtered client-side |
| **Full historical profile** | Same metrics, all-time published Our Friends | `scripts/build_audit_format_profile.py` → `data/audit_format_profile.json` |

## Current audit window

- **After (UTC):** `2025-04-18T15:15:54`
- **Rolling days (if used):** 365
- **Posts matched:** 974
- **Posts with rendered HTML:** 974
- **WP URL:** `https://www.culturaldaily.com`
- **Author ID (Our Friends):** 19

## Empirical aggregates (this window)

| Metric | Value |
|--------|-------|
| `h2_total` | 5955 |
| `h2_numbered_total` | 307 |
| `numbered_h2_fraction` | 0.05155 |
| `ul_total` | 916 |
| `ol_total` | 165 |
| `li_total` | 3673 |
| `inter_tag_gap_single` | 38935 |
| `inter_tag_gap_double` | 0 |
| `inter_tag_gap_triple_plus` | 0 |

## Thresholds suggested for `format_to_audit_standard` / profile JSON

These mirror `audit_format_profile.json` logic; refresh both periodically.

| Key | Value |
|-----|-------|
| `strip_h2_leading_ordinals` | True |
| `numbered_h2_fraction_observed` | 0.05155 |
| `collapse_runs_of_newlines_ge_3` | True |
| `max_serialized_newline_run` | 2 |
| `lists_ul_share_observed` | 0.84736 |

## Top tag counts (published body HTML)

| Tag | Count (aggregate) |
|-----|------------------:|
| `<span>` | 23004 |
| `<p>` | 20838 |
| `<h2>` | 5955 |
| `<a>` | 3689 |
| `<li>` | 3673 |
| `<strong>` | 2965 |
| `<h3>` | 2319 |
| `<b>` | 1524 |
| `<td>` | 1505 |
| `<hr>` | 963 |
| `<ul>` | 916 |
| `<br>` | 858 |
| `<em>` | 822 |
| `<tr>` | 471 |
| `<i>` | 206 |
| `<ol>` | 165 |
| `<h4>` | 141 |
| `<img>` | 118 |

## Top formatting patterns (ranked)

1. **spacing:inter_tag_single_newline** — Serialized `>\n<` gaps (aggregate): 38935
2. **tag:span** — Element <span> appears 23004 times across analyzed posts (aggregate).
3. **tag:p** — Element <p> appears 20838 times across analyzed posts (aggregate).
4. **tag:h2** — Element <h2> appears 5955 times across analyzed posts (aggregate).
5. **h2_structure:h2>#text** — H2 child structure “h2>#text” occurs 4031 times (skeleton, text ignored).
6. **tag:a** — Element <a> appears 3689 times across analyzed posts (aggregate).
7. **tag:li** — Element <li> appears 3673 times across analyzed posts (aggregate).
8. **links:anchor_inside_p** — Anchors nested under <p> (aggregate): 3598
9. **tag:strong** — Element <strong> appears 2965 times across analyzed posts (aggregate).
10. **inline:strong_inside_p** — <strong> inside <p> (aggregate): 2675
11. **tag:h3** — Element <h3> appears 2319 times across analyzed posts (aggregate).
12. **h2_structure:h2>span** — H2 child structure “h2>span” occurs 1691 times (skeleton, text ignored).
13. **tag:b** — Element <b> appears 1524 times across analyzed posts (aggregate).
14. **tag:td** — Element <td> appears 1505 times across analyzed posts (aggregate).
15. **tag:hr** — Element <hr> appears 963 times across analyzed posts (aggregate).
16. **tag:ul** — Element <ul> appears 916 times across analyzed posts (aggregate).
17. **tag:br** — Element <br> appears 858 times across analyzed posts (aggregate).
18. **tag:em** — Element <em> appears 822 times across analyzed posts (aggregate).
19. **tag:tr** — Element <tr> appears 471 times across analyzed posts (aggregate).
20. **tag:i** — Element <i> appears 206 times across analyzed posts (aggregate).

## Rules for new WordPress drafts (human checklist)

1. **Body HTML** comes from the **Google Doc export** + pipeline transforms—not from freeform pasting in WP.
2. **Sponsored body links** must match the hard shape in `QA.md` (bold + `target=_blank`, dofollow).
3. **Spacing:** corpus favors **single-line** gaps between tags; pipeline collapses runaway newlines.
4. **H2:** if most live posts avoid leading `1.` / `2)` markers, expect `format_to_audit_standard` to strip ordinals when the profile says so.
5. **Images:** hero is featured image; in-article images follow `CD-InsertN` naming + centered figure markup after `remediate-latest` / publish path.
6. **When in doubt:** run `python3 pipeline.py remediate-latest cd` on the draft and fix QA failures before calling the job done.

## Exceptions

- **Numbered H2s** appear on a minority of posts (~**5.2%** of H2s in this window). The pipeline may still normalize ordinals toward the dominant style—verify with stakeholders if a client insists on numbers.

## Refresh

```bash
python3 scripts/audit_sponsored_last_year.py --days 365 \
  --out-json data/sponsored_last_year_audit.json \
  --out-md /Users/samuelshemin/Desktop/scoutmonkeys-dashboard/docs/CULTURAL_DAILY_SPONSORED_FORMAT_GUIDE.md
```

